// Central contact configuration. Replace these placeholder values once —
// every WhatsApp button and contact link on the site reads from here.
// Do not hardcode a phone number, WhatsApp number or email anywhere else.
export const contact = {
  // Digits only, with country code, e.g. "919876543210". Leave the
  // placeholder until the real number is available.
  whatsapp: 'YOUR_WHATSAPP_NUMBER',
  phone: '',
  email: '',
}
