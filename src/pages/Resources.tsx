import ResourceListPage from '../components/ResourceListPage'

export default function Resources() {
  return (
    <ResourceListPage
      title="All Resources"
      description="Browse every note, solution, question set, worksheet and previous paper in one place."
      breadcrumbLabel="Resources"
    />
  )
}
